# FIX (B-15, 18-sep-2026): antes no hacía falta .gitignore (no había
# package.json). node_modules/ no se versiona; npm ci lo reconstruye en
# cada corrida de CI desde package-lock.json.
node_modules/
